version: '1.0'
services:
  app:
    build: .
    ports:
      - "8080:8080"
    depends_on:
      - db
  db:
    image: mcr.microsoft.com/mssql/server:2019-latest
    environment:
      - SA_PASSWORD=p@ssword24
      - ACCEPT_EULA=Y
    ports:
      - "1433:1433"

